# Product Requirements

## Purpose

<TODO(template): what this project does and why it exists — one or two
paragraphs. This is the doc CLAUDE.md points to before any structural
change, so keep it current as the design evolves.>

## Users

<TODO(template): who uses this — end users, other services, CI, an
agent/client? What does each of them need from it?>

## Requirements

<TODO(template): the must-have behaviors, invariants, and constraints.
Prefer a numbered list you can reference by number elsewhere — CLAUDE.md's
"Architecture invariants" should trace back to entries here.>

## Non-goals

<TODO(template): what this project explicitly does not do, and why — the
scope boundaries that keep it small.>

## Open questions

<TODO(template): unresolved design decisions, tracked here until answered
and folded into Requirements.>
